<div id="allitems">
	<h1>Today's Orders</h1>
	<% #link_to raw(image_tag('sendemailbutton.png')), notify_admin_users_path, class: 'addnew' %>
	<hr />
	
	<table width="100%">
		<tr>
			<th>User</th>
			<th>Order Description</th>
			<th>Amount</th>
			<th>Delivery Time</th>
			<th>Redeemed?</th>
		</tr>
		<% @sorted_orders.each do |order| %>
			<tr class="<%= cycle('oddrow', 'evenrow') %>">

			</tr>
		<% end %>
	</table>

	<%= @sorted_orders %>




</div>
