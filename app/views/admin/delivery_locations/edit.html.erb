<%= link_to "< Back", admin_delivery_locations_path %>

<%= form_for [:admin, @delivery_location], multipart: true do |f| %>
	<%= render :partial => "form", locals: { delivery_location: @delivery_location, f: f } %>
	<div>
		<%= f.submit "Update" %>
	</div>
<% end %>
